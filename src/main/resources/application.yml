spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgressql://svc.gksl2.cloudtype.app:31068/uriharu
    username: ${root}
    password: ${02070824}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true
